version: 2

models:
  - name: dim_orders
    description: '{{ doc("dim_orders") }}'

    columns:
      - name: order_id
        description: ""
        tests:
            - unique
            - not_null
